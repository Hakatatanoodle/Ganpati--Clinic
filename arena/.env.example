# Copy to .env to enable live LLMs. The dashboard Settings modal can also set
# keys at runtime (in-memory only). Mock players need no keys.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
XAI_API_KEY=
